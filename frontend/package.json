{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",        // Development server (not used for deployment)
    "build": "vite build", // Production build command
    "preview": "vite preview" // Preview the build locally (not needed for Render)
  },
  "dependencies": {
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.5.0"
  }
}
